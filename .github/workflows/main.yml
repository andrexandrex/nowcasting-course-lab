name: First Workflow
on: 
  workflow_dispatch:
jobs:
  basic:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello World"
      - name: Print ioi
        run: echo "DOne" 
    
    
